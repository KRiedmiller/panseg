# Write the handoff spec

Type: task
Status: open
Blocked by: 02, 03, 04, 05, 06, 07, 08

## Question

Produce the destination artifact: `.scratch/time-dimension/spec.md` — a single implementation-ready spec synthesizing all recorded decisions (tickets 02–08): axis order (02), data model (03), loop architecture (04), I/O (05), pipeline (06), viewer/GUI/headless (07), tests/docs (08), with the explicit non-goals (out-of-scope list) and the per-frame label-ID semantics stated. Cross-checked against the codebase so an implementer can pick it up without re-deciding anything.
